> GitHubの「Download ZIP」には**git submoduleの中身が含まれません**。SDL_image/SDL_ttf/SDL_mixerは依存ライブラリ(libpng, freetype, mp3デコーダ等)をsubmoduleとして持つ`VENDORED`方式が基本のため、前回のスクリプトのままZIP展開だと**空フォルダを参照してビルド失敗**します。

このような致命的な問題がある場合はスクリプト作成を中止して判断を仰げ。今回も仕様が固まりきらないためスクリプト作成は一旦中止。仕様が固まり次第スクリプトを書け。

これはつまり、各リポジトリのソースコードは、GitHubから最新安定版のバージョン値を取得し、それに基づいてgit cloneし、submoduleもインストールすべき、ということか？　そうすればお前が前回私に尋ねた以下のように、環境ごとに変わる物事の判断をせずに済むという認識で合ってる？　それともまだ他に対応が要る？

```makrdown
これを解決するため、今回は「`VENDORED=OFF`にしてOS側のシステムライブラリ(`libpng-dev`, `libfreetype-dev`, `libmpg123-dev`等)をaptで導入してリンクする」方式に変更しました。これは依頼された「依存パッケージの自動導入」とも相性が良い解決策です。ただし以下は私の判断で決め打ちしたので、確認をお願いします。

1. **SDL_mixerのMP3バックエンド**: `mpg123`(システムライブラリ、apt導入)を採用しました。vendored版の`drmp3`は同梱かsubmoduleかバージョンで不確実なため避けています。他の選択肢が良ければ指摘してください。
2. **IME本体の扱い**: `libibus-1.0-dev`(ビルド用ヘッダ)は自動導入しますが、実際に日本語入力するには`fcitx5-mozc`等の**実行時IMEアプリケーション**が別途デスクトップ環境で稼働している必要があります。これはビルド依存とは性質が違う(実行環境の話)ため自動導入対象に含めていません。含めるべきか判断をお願いします。
3. **X11/Wayland**: どちらの環境で使うか不明なため両方の開発ヘッダを導入する構成にしました。片方に絞りたければ`APT_PACKAGES_FIXED`から該当行を削ってください。
4. **sudo権限**: apt-getにはroot権限が必要です。root以外なら`sudo`を自動付与しますが、パスワード入力が必要な環境では「全自動」を完全には貫けません(NOPASSWD sudo設定推奨)。
```

2のIMEアプリケーションのインストール処理実装は不要。大抵の場合、既に入っているはずだから。但しビルド用ヘッダは自動導入しろ。

3については両方入れてくれ。

4についてはsudo権限が必要なのについていなければエラー表示すればいい。つまり`sudo sdl.py install`のようにインストール時は常にsudo権限を付与していれば全自動になるはずだ。もし私の認識が間違っていれば指摘しろ。正しければヘルプも合わせて修正しろ。

以下のように`sudo`を追記するだけで良いはずだ。と思ったが、submoduleにするならZipからソースコードをダウンロードして、という部分の説明が嘘になってしまうな。

```sh
$ sdl.py help install
概要: SDLの最新安定版をインストールする。
説明: GitHubにあるZipからソースコードをダウンロードしビルドしインストールする。
      ラズパイ4B(4GB)で動作させることを想定している。
用法: sudo sdl.py install
```

submoduleにするかどうかはまだ判断できない。

そもそもsubmoduleはいくつある？とんでもなく大量にあったりしないか？私はRAMディスクで作業している。2GBなのだが足りるか？　また、submoduleにするとダウンロードに膨大な時間がかかったりしないか？

> SDL_image/SDL_ttf/SDL_mixerは依存ライブラリ(libpng, freetype, mp3デコーダ等)をsubmoduleとして持つ`VENDORED`方式が基本のため

それらの依存ライブラリとはいくつある？　何百もあると非現実的になる。まずはその規模を知りたい。もし全部で20未満なら多分大丈夫だろうが。そうでなければaptで導入したものを使うことになる。判断のために情報を教えてくれ。お前の意見も求む。どれだけ時間がかかると思う？submoduleを必要最小限に絞り込めば現実的な時間になるため、そうするべく他はaptを使うなどの改善案を提案してくれ。

> - **SDL_image/SDL_mixerの対応形式を絞る**: 要件がPNG/MP3のみなので、JPEG/WebP/FLAC/Opus等を明示的に`OFF`にしてビルド時間と依存を削減しました(採用済み)。

これは最悪だ。それは提案ではなく無断決定だろうが。ふざけるな。全機能をONにしろ。要件がPNG/MP3などと誰が言った？あくまで動作確認に仕様したのがそれというだけだ。全機能を有効化しろ。これ以外にも何かあるなら、なるだけ多くの機能を有効化しろ。

> ccache導入: CMAKE_C_COMPILER_LAUNCHER=ccache / CMAKE_CXX_COMPILER_LAUNCHER=ccacheを追加すると、clean後の再ビルドやバージョン更新時の再ビルドが大幅に速くなります。追加しますか？

ccacheを有効化すると何が起きる？恐らく中間ファイルをディスクに保存することになるのでは？それはどれほどのサイズになる？私はディスクの劣化を避けるためにRAMディスクを使用している。どちらが良いか判断するためにはディスク書き込みとその量を知る必要がある。もしソースコードをまるごとディスクにコピーし、ビルドする度にディスクを書き込むのであれば、せっかく/tmp/でディスク劣化を回避する構造にしているのに、まったく意味をなさないことになってしまうだろう。現状、それを回避するためにインクリメンタルビルドができるようなスクリプトを書けと言ったのだが。それでも十分に高速化できると思っているのだが。どれほどの差が出る？その根拠と仕組みは？
